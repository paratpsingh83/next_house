# nexthouse-frontend-v3 — environment variables
# Copy to .env.local and fill in your values. NEVER commit .env.local to git.

# Backend API base URL (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:8080